rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 단계: 모든 읽기/쓰기 허용
    // TODO: 프로덕션 배포 전 보안 규칙 재적용
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
